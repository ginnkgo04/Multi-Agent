{
  "name": "multi-agent-platform",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm --workspace @multi-agent/web run dev",
    "build:web": "npm --workspace @multi-agent/web run build",
    "lint:web": "npm --workspace @multi-agent/web run lint"
  }
}
